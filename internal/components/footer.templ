package components

import "time"

templ footerTemplate(title string) {
    {{year := time.Now().Year()}}

    <footer class="footer-banner">
        <div class="footer-content">
            {title} ©{year}
        </div>
    </footer>
}